# AICC CDP Port Badge

Minimal Manifest V3 identity extension for configured AICC CDP slots. It uses
only `chrome.storage.local`; it has no host, tab, history, cookie, content-script,
or remote-code permission.

Install it only through a verified CDP endpoint. The slot value is local to the
browser profile.
